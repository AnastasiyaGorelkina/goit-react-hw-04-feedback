import styled from '@emotion/styled';

const Notific = styled.p`
  font-size: 35px;
  font-weight: bold;
  background: linear-gradient(150deg, red 10%, orange 20%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  display: block;
  padding: 0.3em 0.6em;
`;

export default Notific;